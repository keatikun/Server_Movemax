Flask==2.3.2  # ใช้เวอร์ชันที่คุณติดตั้ง หรือเวอร์ชันล่าสุดที่เสถียร
Flask-SocketIO==5.3.0 # ใช้เวอร์ชันที่คุณติดตั้ง หรือเวอร์ชันล่าสุดที่เสถียร
eventlet==0.33.0 # จำเป็นสำหรับ Flask-SocketIO production server
python-engineio==4.8.0 # Dependency ของ Flask-SocketIO
python-socketio==5.11.0 # Dependency ของ Flask-SocketIO
pymongo==4.6.1 # สำหรับเชื่อมต่อ MongoDB
Flask-Cors==4.0.0 # สำหรับจัดการ CORS

# หากคุณใช้ python-dotenv เพื่อโหลด SECRET_KEY หรือ MONGO_URI จาก .env ไฟล์
# python-dotenv==1.0.1
