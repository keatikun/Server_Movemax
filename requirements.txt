Flask==2.3.2  # หรือเวอร์ชันที่ใกล้เคียงกับที่คุณใช้
Flask-SocketIO==5.3.0 # หรือเวอร์ชันที่เข้ากันได้
eventlet==0.33.0 # หรือเวอร์ชันที่เข้ากันได้ (สำคัญสำหรับ Flask-SocketIO production server)
python-engineio==4.8.0 # Dependencies ของ Flask-SocketIO
python-socketio==5.11.0 # Dependencies ของ Flask-SocketIO
pymongo==4.6.1 # สำหรับเชื่อมต่อ MongoDB
Flask-Cors==4.0.0 # สำหรับจัดการ CORS

# หากคุณใช้ python-dotenv เพื่อโหลด SECRET_KEY หรือ MONGO_URI จาก .env ไฟล์
# python-dotenv==1.0.1
